{% macro nav_link(current, target, label) %}
    <li class="nav-item">
        <a class="nav-link{{ current == target ? ' active' }}" href="{{ path(target) }}">{{ label }}</a>
    </li>
{% endmacro %}

<nav class="navbar navbar-expand-lg navbar-dark bg-dark">
    <div class="container-fluid">
        <a class="navbar-brand" href="{{ path('app_main_index') }}">Sensio TV+</a>
        <ul class="navbar-nav me-auto mb-2 mb-lg-0">
            {{ _self.nav_link(app.current_route, 'app_main_index', 'Home') }}
            {{ _self.nav_link(app.current_route, 'app_book_index', 'Book') }}
            {{ _self.nav_link(app.current_route, 'app_movie_index', 'Movie') }}
            {{ _self.nav_link(app.current_route, 'app_main_contact', 'Contact') }}
        </ul>
    </div>
</nav>
